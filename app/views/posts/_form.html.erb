<%= form_with(model: post) do |form| %>
  <% if post.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>
      <ul>
        <% post.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div>
    <%= form.label :title, class: "user_color", style: "display: block" %>
    <%= form.text_field :title, class: "form-control", id: "exampleInputTitle" %>
  </div>
  <div>
    <%= form.label :body, class: "user_color", style: "display: block" %>
    <%= form.rich_text_area :body, class: "form-control", id: "exampleInputBody" %>
  </div>
  <div>
    <%= form.submit class: "btn btn-success"%>
  </div>
<% end %>
